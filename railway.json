{
  "buildCommand": "python -m pip install -r requirements.txt",
  "startCommand": "python manage.py migrate --noinput && python manage.py collectstatic --noinput && gunicorn backend.wsgi:application"
}
